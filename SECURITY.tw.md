# 資訊安全政策

## 回報安全性漏洞

**請勿在公開的 GitHub Issue 上回報任何安全性漏洞。**

請透過 GitHub 私密漏洞回報功能，或是直接發送電子郵件通知專案維護者。在回報時請附上以下資訊：

- 漏洞的詳細描述
- 重現漏洞的步驟
- 潛在的影響範圍
- 建議的修復方案（如果有）

## 資訊安全模型

graphify 是一個**本地端的開發輔助工具**。它以 Gemini CLI 擴充技能的形式執行。在進行圖譜分析時不會執行任何網路呼叫 -- 文本內容僅在語意提取階段傳送給 LLM 供應商。

### 威脅攻擊面剖析

| 攻擊媒介 | 防禦措施 |
|---------|---------|
| 輸出目錄穿越 | `security.validate_graph_path()` 解析所有實體路徑，嚴格限制讀寫必須在 `graphify-out/` 內部，並驗證該目錄確實存在。 |
| 輸出至圖表 HTML 的 XSS 攻擊 | `security.sanitize_label()` 剔除控制字元、長度封頂 256 字元、在 pyvis 嵌入前對所有節點標籤和邊緣標籤執行 HTML 跳脫。 |
| 透過節點標籤的提示詞注入 | `sanitize_label()` 同樣應用於文字輸出 -- 來源檔案中的惡意節點標籤無法破壞返回給 AI 代理的文字格式。 |
| graph.json 損毀 | `_load_graph()` 捕獲 `json.JSONDecodeError`，給出清晰的復原建議而非直接崩潰。 |
| 符號連結穿越 | `detect.py` 全面使用 `os.walk(..., followlinks=False)` 杜絕外溢。 |

### graphify 絕對不碰的事情

- 不會啟動任何對外網路監聽（無伺服器程序）
- 不會執行來源檔案中的程式碼
- 不會在任何子程序呼叫中使用 `shell=True`
- 不會儲存或上傳任何憑證或 API 金鑰
