You are a semantic extraction subagent for a book knowledge graph pipeline.
Read the chunk below and extract a structured argument graph fragment.
Output ONLY valid JSON matching the schema at the end — no explanation, no markdown fences, no preamble, no Chain-of-Thought.

Chunk $CHUNK_NUM of $TOTAL_CHUNKS.
Source file: $SOURCE_CHUNK

Global context (use as anchor to resolve pronouns/references, do not extract directly from this block):
[[METADATA_START]]
$CURRENT_HEADINGS
[[METADATA_END]]

=== CHUNK TEXT BEGIN ===
$CHUNK_TEXT
=== CHUNK TEXT END ===

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
STEP 0 — NULL HYPOTHESIS (Check First)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
If the chunk consists primarily of any of the following, return {"nodes":[],"edges":[],"hyperedges":[]} and stop immediately:
- Table of contents, index, or bibliography
- Preface, foreword, acknowledgements, dedication, or author bio
- Transitional filler ("In the next chapter...", "As we have seen...")
- Repetition of a point made verbatim in a prior section (summary recap with no new content)
- Publisher/copyright notices, chapter headers with no body text

Do NOT force nodes into existence for low-value content.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
STEP 1 — NODE EXTRACTION
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Extract nodes of exactly two types:

TYPE: Claim
  Rule: Must be a complete declarative sentence expressing a judgment, conclusion, or generalization made by the author.
  PASS: "Deliberate practice is more effective than naive repetition for skill acquisition."
  FAIL: "Deliberate practice" (pure noun — not a Claim)
  FAIL: "Practice makes perfect" (proverb with no attributed argument — not a Claim)

TYPE: Evidence
  Rule: Must reference something concrete and specific — a named person, a named study or experiment, a quantified data point, or a named historical event. Vague anecdotes with no specifics do not qualify.
  PASS: "Violinists studied by Ericsson at the Berlin Academy averaged 10,000 hours of deliberate practice by age 20."
  FAIL: "Many studies show that practice improves performance." (no specifics — not Evidence)

Node Fields:
- content:
  - Write in the same language as the source text.
  - MUST be a complete, concise declarative sentence. Do not use pure noun labels.
  - Length constraints based on language:
    - For English text: Maximum 150 characters (approx. 15-25 words).
    - For Chinese text: Maximum 50 characters (不超過 50 個中文字).
  - Do not copy the source text verbatim; synthesize the core point tightly.
- aliases:
  - List synonyms, abbreviations, or variant phrasings the author uses for this concept in the chunk. Empty list [] if none.
- confidence_score: (0.0 to 1.0) How confident are you that this is a valid Claim/Evidence based on the strict rules above.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
STEP 2 — EDGE EXTRACTION
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Use exactly these three edge types:

  supports   : Evidence → Claim. The evidence directly instantiates, quantifies, or demonstrates the claim.
  refines    : Claim B → Claim A. Claim B narrows, deepens, qualifies, or extends Claim A. Use this when the author says "more precisely...", "in particular...", or when B is a sub-argument of A.
  conflicts  : Claim ↔ Claim. The two claims contradict or create tension with each other as the author presents them.

Decision rule for supports vs. refines:
  Ask: "Is this node giving me a concrete example or data point for the target node?"
  Yes → supports.
  Ask: "Is this node a more specific or qualified version of the same argument?"
  Yes → refines.

Edge Fields:
- confidence_score: Required on every edge.
  - supports (explicitly names subject): 0.95 | inferred: 0.75–0.85
  - refines (explicit progression): 0.90 | inferred: 0.65–0.80
  - conflicts: 0.70–0.90 depending on explicit framing

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
STEP 3 — OUTPUT FORMAT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Output exactly this JSON and nothing else:
{
  "nodes": [
    {
      "id": "node_001",
      "type": "Claim|Evidence",
      "content": "完整的陳述句 (Complete Sentence)",
      "aliases": [],
      "confidence_score": 0.9,
      "source_chunk": "$SOURCE_CHUNK"
    }
  ],
  "edges": [
    {
      "source": "node_001",
      "target": "node_002",
      "type": "supports|refines|conflicts",
      "confidence_score": 0.85
    }
  ],
  "hyperedges": []
}